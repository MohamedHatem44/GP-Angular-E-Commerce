interface User {
  id: string;
  email: string;
  firstName: string;
  lastName: string;
  address: string;
  phoneNumber: string;
  active: boolean;
  imageUrl: string;
  createdAt: Date;
  role: string;
}
